{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "name": "Debug NestJS",
      "request": "launch",
      "runtimeArgs": [
        "--nolazy",
        "-r",
        "ts-node/register",
        "-r",
        "tsconfig-paths/register"
      ],
      "args": ["${workspaceFolder}/src/main.ts"],
      "cwd": "${workspaceFolder}",
      "restart": true
    }
  ]
}
